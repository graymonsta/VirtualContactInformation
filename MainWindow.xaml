<Window x:Class="VirtualContactInformation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VirtualContactInformation"
        mc:Ignorable="d"
        Title="Virtual Contact Information" Height="450" Width="800">
    <Grid Margin="10" Background="#FFD1F3EF">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        
        <StackPanel Grid.Column="0" Margin="10,108,10,108">
            <TextBlock Text="Nombre:"/>
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Margin="0,10,0,0"><Run Language="es-419" Text="Teléfono"/><Run Text=":"/></TextBlock>
            <TextBox Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Margin="0,10,0,0"><Run Text="Email"/><Run Language="es-419" Text=":"/></TextBlock>
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"/>

            <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                <Button Content="Agregar" Command="{Binding AddCommand}" Width="80"/>
                <Button Content="Actualizar" Command="{Binding UpdateCommand}" Width="80" Margin="10,0,0,0"/>
                <Button Content="Eliminar" Command="{Binding DeleteCommand}" Width="80" Margin="10,0,0,0"/>
            </StackPanel>
        </StackPanel>
        <ListBox Grid.Column="1"
                 ItemsSource="{Binding Contacts}"
                 SelectedItem="{Binding SelectedContact, Mode=TwoWay}"
                 Margin="10">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                        <TextBlock Text="{Binding Phone}"/>
                        <TextBlock Text="{Binding Email}" FontStyle="Italic"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>